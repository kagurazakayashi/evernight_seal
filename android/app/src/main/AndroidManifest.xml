<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="EvernightSeal"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- 指定一個 Android 主題，在 Android 程序啟動後立即套用至此 Activity。
                 此主題在 Flutter UI 初始化期間對使用者可見。
                 Flutter UI 初始化完成後，此主題將繼續決定 Flutter UI
                 後方的視窗背景。 -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- 請勿刪除下方的 meta-data。
             此為 Flutter 工具用於產生 GeneratedPluginRegistrant.java 所使用。 -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- 查詢可處理文字的 Activity 所必需，請參閱：
         https://developer.android.com/training/package-visibility 和
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT。

         特別注意，此查詢被 Flutter 引擎用於 io.flutter.plugin.text.ProcessTextPlugin。 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
